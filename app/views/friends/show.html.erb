<p style="color: green"><%= notice %></p>



<%= render @friend %>

<div class="container">
    <div class="bg-info text-white p-5 my-3">
    <%= link_to "Edit", edit_friend_path(@friend), class:"btn btn-dark" %> |
    <%= link_to "Back", friends_path , class:"btn btn-secondary" %>|
    <%= link_to "Destroy", @friend, data: { turbo_method: :delete, turbo_confirm: "Are you sure?"},  class:"btn btn-dark" %>
    </div>
</div>
